import { Box, Text } from "@chakra-ui/react";

const Captains = () => {
  return (
    <Box paddingTop={90} className="innerWidth">
      <Text>Captains</Text>
    </Box>
  );
};

export default Captains;
